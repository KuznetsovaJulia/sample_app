<% provide(:title, 'All users') %>
<h1>All users</h1>

<ul class="users">
  <% @users.each do |user| %>
    <li>
      <%= image_tag user.avatar.thumb.url %>
      <%= link_to user.name, user %>
    </li>
  <% end %>
</ul>